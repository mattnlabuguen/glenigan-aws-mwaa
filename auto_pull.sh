#!/bin/bash

# Navigate to the repository directory
cd ~/glenigan-aws-mwaa

# Fetch changes from the remote repository
git fetch 

# Pull changes from the remote repository's main branch
git pull origin main
